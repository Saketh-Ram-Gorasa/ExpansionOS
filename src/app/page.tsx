import { ExpansionShowcase } from "@/components/ExpansionShowcase";

export default function Home() {
  return (
    <ExpansionShowcase
      theme="core"
      title="ExpansionOS // Core Intelligence Console"
      subtitle="Find the next adjacent segment for your startup and return the narrowest MVP wedge your team can prove this quarter."
      introMetric="CORE MODE"
    />
  );
}
