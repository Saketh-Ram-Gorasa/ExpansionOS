import { ExpansionShowcase } from "@/components/ExpansionShowcase";

export default function MissionControlPage() {
  return (
    <ExpansionShowcase
      theme="mission-control"
      title="ExpansionOS // Mission Control"
      subtitle="One-command playbook for adjacent segment expansion: signal triage, wedge validation, and proof commitments."
      introMetric="COMMAND MODE"
    />
  );
}
