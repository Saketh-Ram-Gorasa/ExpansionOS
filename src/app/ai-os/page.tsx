import { ExpansionShowcase } from "@/components/ExpansionShowcase";

export default function AiOsPage() {
  return (
    <ExpansionShowcase
      theme="ai-os"
      title="ExpansionOS // AI OS"
      subtitle="Coordinated agents, live Crustdata calls, and a single explainable decision card for the wedge you should build."
      introMetric="AI OS MODE"
    />
  );
}
