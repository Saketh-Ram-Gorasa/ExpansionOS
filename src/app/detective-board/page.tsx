import { ExpansionShowcase } from "@/components/ExpansionShowcase";

export default function DetectiveBoardPage() {
  return (
    <ExpansionShowcase
      theme="detective-board"
      title="ExpansionOS // Evidence Board"
      subtitle="Trace every adjacent-segment recommendation with chain-of-custody proof and audit-ready sources."
      introMetric="EVIDENCE MODE"
    />
  );
}
